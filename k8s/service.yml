apiVersion: v1
kind: Service
metadata:
  name: cxer
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cxer"
    port: 8080
  selector:
    app: cxer
