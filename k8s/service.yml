apiVersion: v1
kind: Service
metadata:
  name: vnghn
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "vnghn"
    port: 8080
  selector:
    app: vnghn
