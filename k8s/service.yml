apiVersion: v1
kind: Service
metadata:
  name: bsvf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bsvf"
    port: 8080
  selector:
    app: bsvf
