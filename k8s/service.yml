apiVersion: v1
kind: Service
metadata:
  name: cdvrg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cdvrg"
    port: 8080
  selector:
    app: cdvrg
