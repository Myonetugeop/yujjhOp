apiVersion: v1
kind: Service
metadata:
  name: xzdcds
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xzdcds"
    port: 8080
  selector:
    app: xzdcds
