apiVersion: v1
kind: Service
metadata:
  name: vrzc
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "vrzc"
    port: 8080
  selector:
    app: vrzc
