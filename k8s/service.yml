apiVersion: v1
kind: Service
metadata:
  name: ikop
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ikop"
    port: 8080
  selector:
    app: ikop
