apiVersion: v1
kind: Service
metadata:
  name: vfrtg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "vfrtg"
    port: 8080
  selector:
    app: vfrtg
