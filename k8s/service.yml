apiVersion: v1
kind: Service
metadata:
  name: vffes
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "vffes"
    port: 8080
  selector:
    app: vffes
