apiVersion: v1
kind: Service
metadata:
  name: oldf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "oldf"
    port: 8080
  selector:
    app: oldf
