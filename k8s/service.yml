apiVersion: v1
kind: Service
metadata:
  name: mvjd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mvjd"
    port: 8080
  selector:
    app: mvjd
