apiVersion: v1
kind: Service
metadata:
  name: tfas
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "tfas"
    port: 8080
  selector:
    app: tfas
