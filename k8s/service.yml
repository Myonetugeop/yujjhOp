apiVersion: v1
kind: Service
metadata:
  name: fbts
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "fbts"
    port: 8080
  selector:
    app: fbts
