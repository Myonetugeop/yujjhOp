apiVersion: v1
kind: Service
metadata:
  name: bgrs
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bgrs"
    port: 8080
  selector:
    app: bgrs
