apiVersion: v1
kind: Service
metadata:
  name: bdsg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bdsg"
    port: 8080
  selector:
    app: bdsg
