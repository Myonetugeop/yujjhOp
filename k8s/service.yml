apiVersion: v1
kind: Service
metadata:
  name: odjs
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "odjs"
    port: 8080
  selector:
    app: odjs
