apiVersion: v1
kind: Service
metadata:
  name: hehds
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hehds"
    port: 8080
  selector:
    app: hehds
