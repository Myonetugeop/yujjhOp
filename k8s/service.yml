apiVersion: v1
kind: Service
metadata:
  name: bfvgrt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bfvgrt"
    port: 8080
  selector:
    app: bfvgrt
