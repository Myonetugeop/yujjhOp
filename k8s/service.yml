apiVersion: v1
kind: Service
metadata:
  name: wcer
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wcer"
    port: 8080
  selector:
    app: wcer
