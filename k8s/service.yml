apiVersion: v1
kind: Service
metadata:
  name: xcvrr
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xcvrr"
    port: 8080
  selector:
    app: xcvrr
