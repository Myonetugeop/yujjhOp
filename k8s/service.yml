apiVersion: v1
kind: Service
metadata:
  name: bfgtt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bfgtt"
    port: 8080
  selector:
    app: bfgtt
