apiVersion: v1
kind: Service
metadata:
  name: frbd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "frbd"
    port: 8080
  selector:
    app: frbd
