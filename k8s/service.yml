apiVersion: v1
kind: Service
metadata:
  name: nythtyt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "nythtyt"
    port: 8080
  selector:
    app: nythtyt
