apiVersion: v1
kind: Service
metadata:
  name: tjki
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "tjki"
    port: 8080
  selector:
    app: tjki
